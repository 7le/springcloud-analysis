spring:
  application:
    name: spring-cloud-ad
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        healthCheckPath: /status
        healthCheckInterval: 5s
server:
  port: 8000
